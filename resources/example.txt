start
def main ():
{
    input (x)
    input (v)
    y = x / v;
    output (y)
    return 0;
}
def sum (a, b): {
    x = 5;
    return a+b;
}
def mul (a, b): {
    x = 5;
    return a*b;
}
end